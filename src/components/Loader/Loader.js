import React from 'react';
import { LoaderContainer, LoaderSpinner } from './Loader.styles';

const Loader = () => {
  return (
    <LoaderContainer>
      <LoaderSpinner />
    </LoaderContainer>
  );
};

export default Loader;
